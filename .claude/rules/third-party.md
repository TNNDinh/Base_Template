---
trigger: always_on
---

# 3RD PARTY LIB
**DOTween:** All code anims. Handle `OnComplete`/`Kill`. UI: `SetUpdate(true)` (Pause-safe).
**Odin:** Use attrs (`[Title]`, `[TabGroup]`, `[ShowIf]`, `[Required]`, `[Button]`).
**Events:** `TigerForge`. Use `EventName` constants (loose coupling).
