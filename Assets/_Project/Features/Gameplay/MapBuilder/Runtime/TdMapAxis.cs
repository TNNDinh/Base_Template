namespace Ezg.Feature.MapBuilder
{
    /// <summary>
    /// Trục đặt cặp Spawn/Goal đối diện nhau.
    /// </summary>
    public enum TdMapAxis
    {
        /// <summary>Spawn bên trái, Goal bên phải — cùng một hàng.</summary>
        Horizontal = 0,

        /// <summary>Spawn phía dưới, Goal phía trên — cùng một cột.</summary>
        Vertical = 1,
    }
}
